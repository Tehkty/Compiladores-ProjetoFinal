# Provas de Ambiente

Antes da apresentação, execute os comandos abaixo e salve capturas de tela na pasta `docs/evidencias/`.

```bash
gcc -v
flex --version
bison --version
nasm -v
clang --version
llvm-config --version
```

Também tire prints de:

```bash
make
./run_tests.sh
```
